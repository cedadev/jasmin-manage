[tox]
envlist = clean,py3.9,report

[testenv]
commands = coverage run manage.py test {posargs:jasmin_manage}
setenv =
  DJANGO_SETTINGS_MODULE = jasmin_manage.tests.settings
deps =
  -r requirements.txt
  coverage
depends =
  py3.9: clean
  report: py3.9

[testenv:report]
deps = coverage
skip_install = true
commands =
  coverage report
  coverage html
  coverage xml

[testenv:clean]
deps = coverage
skip_install = true
commands = coverage erase
